<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ActionZone">

        <!-- SplashActivity démarre en premier -->
        <activity android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity : écran principal -->
        <activity android:name=".MainActivity"
            android:exported="false"/>

        <!-- RechercheActivity : recherche TMDB -->
        <activity android:name=".RechercheActivity"
            android:exported="false"/>

        <!-- DetailActivity : détail d'un film -->
        <activity android:name=".DetailActivity"
            android:exported="false"/>

        <!-- ParametresActivity : paramètres -->
        <activity android:name=".ParametresActivity"
            android:exported="false"/>

    </application>

</manifest>